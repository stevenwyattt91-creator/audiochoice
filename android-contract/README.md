# AudioChoice Android contracts

These Kotlin serialization models mirror the Stage 13 backend and Swift contracts.
They are source input for the native Android project created in Stage 14. The Android
toolchain is not installed on the current Mac, so this source has not yet been compiled.
