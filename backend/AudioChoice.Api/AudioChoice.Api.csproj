<Project Sdk="Microsoft.NET.Sdk.Web">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>
  <PropertyGroup Condition="'$(EnablePostgres)' == 'true'">
    <DefineConstants>$(DefineConstants);POSTGRES;GOOGLEAUTH</DefineConstants>
  </PropertyGroup>
  <ItemGroup>
    <!-- The contract tests assert that the generated prompt still covers every
         allowed label, which needs the generated text rather than a copy of it. -->
    <InternalsVisibleTo Include="AudioChoice.Api.ContractTests" />
  </ItemGroup>
  <ItemGroup>
    <Content Include="../Database/Migrations/**/*.sql" Link="Database/Migrations/%(RecursiveDir)%(Filename)%(Extension)" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>
  <ItemGroup Condition="'$(EnablePostgres)' == 'true'">
    <PackageReference Include="Npgsql" Version="10.0.3" />
    <PackageReference Include="Google.Apis.Auth" Version="1.75.0" />
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="QRCoder" Version="1.6.0" />
    <PackageReference Include="Azure.Identity" Version="1.21.0" />
    <PackageReference Include="Azure.Storage.Blobs" Version="12.29.1" />
  </ItemGroup>
</Project>
